title: User
required:
    - username
    - passwordHash
properties:
    username: {type: string}
    passwordHash: {type: string}
    fullName: {type: string}
    avatar: {type: string}
    role: {type: string, enum: [admin, user]}
    _id: {type: string}
